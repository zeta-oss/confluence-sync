# Mermaid Diagram

This page contains a Mermaid flowchart to verify diagram rendering.

```mermaid
flowchart TD
    A[Start] --> B{Is it working?}
    B -->|Yes| C[Great!]
    B -->|No| D[Debug]
    D --> A
```

The diagram above should be rendered as a PNG attachment.

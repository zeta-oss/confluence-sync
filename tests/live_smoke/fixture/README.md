# Smoke Fixture Repo

This is the smoke test fixture repository for confluence-sync.
It is copied to a tmp workspace during `confluence-sync smoke run` and
`nox -s live_smoke` — **never edited in place** (ADR 0024).
